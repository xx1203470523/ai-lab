# wms-dev references

本目录存放 `wms-dev` 的可选参考资料，例如 Task Contract 示例、规则包路由说明、Agent prompt 示例或历史决策。

约束：

- 不承载强约束；强约束必须进入 `../rules/` 或 `../rules/packs/`。
- 不替代 `workflows/`；复杂流程仍放 `workflows/`。
- 与 Rules 冲突时，以 Rules 为准。
