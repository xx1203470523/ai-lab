# Git Worktree 规范

## 创建规范

- 工作区目录统一放在项目根目录下的 `.claude/worktree/` 中
- 工作区命名隔断符 `-`，分支命名隔断符 `/`
- 基于最新 main（main/production）分支创建
- 禁止创建与已有工作区同名的分支

## 删除规范

- 未经用户确认不得删除任何代码或工作区
- 代码改动只限于工作区内
- 禁止删除有未合并代码的工作区
- 禁止删除有未提交改动的工作区
